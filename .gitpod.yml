image:
  file: .gitpod.Dockerfile
tasks:
  - name: Build complete version of snake-fury
    init: git checkout snake-fury-exercise && stack build
    command: |
      export PATH=${PATH}:${HOME}/.local/bin
      echo -e "cradle:\n  stack:" > hie.yaml
vscode:
  extensions:
    - haskell.haskell